# 2026-02-27 开发进度概要

## 📅 项目状态
**项目名称**: woyousha (我有啥)
**当前版本**: MVP (最小可行性产品) 完成
**测试环境**: iOS 17.0+ (真机 + 模拟器)

---

## 🚀 今日核心产出 (Key Features)

### 1. 数据模型与持久化
- **SwiftData 集成**: 完成了 `Item` 数据模型的设计，支持存储图片 (`Data`)、分类 (`Enum`)、数量、位置等信息。
- **类型安全**: 将分类字段重构为 `Category` 枚举，确保数据一致性，并实现了与 `String` 存储的无缝转换。

### 2. 物品录入功能 (`AddItemView`)
- **表单设计**: 实现了包含名称、分类、位置、数量、备注的完整录入表单。
- **图片获取**: 
  - 集成了 **相机** (`UIImagePickerController`) 和 **相册** (`PhotosPicker`)。
  - 优化了交互逻辑，使用统一的“添加照片”入口，点击后通过 ActionSheet 选择来源，解决了 UI 层级冲突问题。
- **AI 智能抠图**: 
  - 封装了 `ImageUtils` 工具类。
  - 利用 iOS 17 `Vision` 框架 (`VNGenerateForegroundInstanceMaskRequest`) 实现了一键去除背景。
  - 增加了**自动描边**效果，生成贴纸风格图片。

### 3. 界面展示 (`ContentView`)
- **首页列表**: 
  - 定制了列表项样式，左侧展示物品缩略图（支持透明背景），右侧展示分类标签和数量。
  - 实现了无图状态下的默认占位图标。
- **详情页面**: 
  - 实现了顶部大图展示（自适应高度）。
  - 优化了信息排版，使用图标 (`Label`) 增强可读性。

---

## 🛠️ 技术亮点与工程优化

### 1. 模拟器与真机兼容性
- **降级保护**: 针对模拟器不支持 Vision 抠图的问题，添加了环境检测 (`#if targetEnvironment(simulator)`) 和错误捕获，自动降级为使用原图，防止 Crash。
- **版本适配**: 将项目 Deployment Target 调整为 iOS 17.0，确保兼容用户设备。

### 2. 稳定性修复
- **数据库迁移**: 修复了开发阶段因模型变更导致的 SwiftData 崩溃问题，添加了 Debug 模式下的自动重置逻辑。
- **编译修复**: 解决了 `CoreImage` 滤镜参数类型 (`Int` vs `Float`) 及 API 调用错误。
- **签名配置**: 移除了免费开发者账号不支持的 Capability (Push Notifications, iCloud)，解决了真机调试签名失败的问题。

---

## 🐛 已修复的关键 Bug
1. **SwiftData Crash**: 解决了 `Fatal error: Could not create ModelContainer` 问题。
2. **UI 层级冲突**: 修复了在 `Menu` 中调用相机导致的 `_UIReparentingView` 报错，改为使用 `confirmationDialog`。
3. **Vision 报错**: 修复了模拟器下 `Could not create inference context` 错误。

---

## 🔜 后续规划 (Next Steps)
1. **搜索功能**: 添加全局搜索，支持按名称或分类查找物品。
2. **数据统计**: 首页增加仪表盘，展示物品总数、分类占比等。
3. **iCloud 同步**: (需开发者账号) 实现多设备数据同步。
4. **分类筛选**: 在首页列表增加分类过滤器。
